import React from "react";
import Loader from "../Components/Loader";

const Main = () => {
    return (
        <main>
            <Loader />
            <div>Home</div>
        </main>
    );
}

export default Main;