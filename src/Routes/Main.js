import React from "react";
import Loader from "../Components/Loader";

const Main = () => {
    return (
        <main>
            <Loader />
            <div>main</div>
        </main>
    );
}

export default Main;